'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var core = require('@vuelidate/core');

var ValidateEach = {
  name: 'ValidateEach',
  props: {
    rules: {
      type: Object,
      required: true
    },
    state: {
      type: Object,
      required: true
    },
    options: {
      type: Object,
      default: () => ({})
    }
  },

  setup(props, {
    slots
  }) {
    const v = core.useVuelidate(props.rules, props.state, props.options);
    return () => slots.default({
      v: v.value
    });
  }

};

exports.ValidateEach = ValidateEach;
