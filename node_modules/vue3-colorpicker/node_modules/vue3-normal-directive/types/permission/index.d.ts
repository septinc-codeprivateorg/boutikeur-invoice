import { PermissionOptions } from "../utils/types";
import { App } from "vue";
declare const permissionPlugin: {
    install: (app: App, options?: PermissionOptions | undefined) => void;
};
export default permissionPlugin;
