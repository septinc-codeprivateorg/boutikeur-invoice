import { App } from "vue";
import { WatermarkPluginOptions } from "../utils/types";
import { WatermarkOption } from "../utils/types";
export declare const useWaterMarker: (parentNode: HTMLElement, opt: WatermarkOption) => void;
declare const waterMarkerPlugin: {
    install: (app: App, options: WatermarkPluginOptions) => void;
};
export default waterMarkerPlugin;
